# Task-Scheduler-System
It a C++ console application that helps users manage tasks efficiently using core Data Structures. It stores tasks in an array, schedules pending tasks with a Priority Queue based on deadlines, maintains completed tasks using a Stack for undo functionality, validates dates, prevents duplicate tasks, and supports task search, deletion, and tracking.
